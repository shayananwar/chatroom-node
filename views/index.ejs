<!DOCTYPE html>
<html>
  <head>
    <title><%= pagedata.title %></title>
    <link rel='stylesheet' href='/stylesheets/<%= pagedata.style %>' />
    <% pagedata.scripts.forEach(function(script) { %>
        <script type="text/javascript" src="/js/<%= script %>"></script>
    <% }); %>
  </head>
  <body>
    <div class="clients"><div class="online-dot"></div> <span class="connected">2</span> online</div>
  	<ul id="messages"></ul>
    <form action="">
      <input id="m" autocomplete="off" /><button>Send</button>
    </form>
    <script>
      var socket = io();
      $('form').submit(function(){
        socket.emit('chat message', $('#m').val());
        $('#m').val('');
        return false;
      });
      socket.on('chat message', function(msg){
        $('#messages').append($('<li>').text(msg));
      });

      // socket.on('clients', function(msg){
      //   $('.connected').html(msg);
      // });
    </script>
  </body>
</html>
